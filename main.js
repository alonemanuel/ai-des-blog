main();
// var canvasDiv = document.getElementById(`myCanvas`);
// var width = canvasDiv.offsetWidth;

function main() {
    createBody();
}

function createBody() {
    console.debug(`creating body`);
    // let mainPanel = document.body.appendChild(document.createElement(`div`));
}